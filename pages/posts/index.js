import AllPosts from 'components/posts/AllPosts';
import PostGrid from 'components/posts/PostGrid';

export default function AllPosts({ allPosts }) {
  return <AllPosts>{/* <PostGrid posts={allPosts} /> */}</AllPosts>;
}
